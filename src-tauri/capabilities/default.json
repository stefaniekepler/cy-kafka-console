{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "default",
  "description": "splash 窗口所需最小权限；main 窗口加载远程 kafbat-ui，故不授予任何能力",
  "windows": ["splash", "settings"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-close"
  ]
}
